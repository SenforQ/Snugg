
//: Declare String Begin

/*: "getDeviceID" :*/
fileprivate let app_backError:String = "getDeself float network as"

/*: "getFirebaseID" :*/
fileprivate let modelFirstData:[Character] = ["g","e","t","F","i","r","e","b","a","s","e","I","D"]

/*: "getAreaISO" :*/
fileprivate let servicePriceResult:[Character] = ["g","e","t","A","r","e","a","I","S","O"]

/*: "getProxyStatus" :*/
fileprivate let modelComponentPartCount:String = "getPused area data where model"
fileprivate let parserFoundationMessage:String = "tatuevaluate"

/*: "getMicStatus" :*/
fileprivate let sessionPreviousError:String = "black margin run command mgetMicS"
fileprivate let sessionOkName:[Character] = ["t","a","t","u","s"]

/*: "getPhotoStatus" :*/
fileprivate let serviceReduceStr:[Character] = ["g","e","t","P","h","o"]
fileprivate let configOriginSourceTitle:String = "T"
fileprivate let dataAppearStatus:String = "oStatusfeedback request string or feedback"

/*: "getCameraStatus" :*/
fileprivate let constSoundSecretName:String = "tab flexible valuegetCa"
fileprivate let dataTapKeyFormat:[Character] = ["a","t","u","s"]

/*: "reportAdjust" :*/
fileprivate let const_programState:String = "rsystempo"

/*: "requestLocalPush" :*/
fileprivate let serviceBounceString:String = "reqexecutes"
fileprivate let appCornerList:[Character] = ["a","l","P","u","s","h"]

/*: "getLangCode" :*/
fileprivate let kMasterKeyTime:[Character] = ["g","e","t","L","a","n","g","C","o"]
fileprivate let kWaitMessage:[Character] = ["d","e"]

/*: "getTimeZone" :*/
fileprivate let userSubData:[Character] = ["g","e","t","T","i","m","e","Z","o","n"]
fileprivate let appStopData:String = "E"

/*: "getInstalledApps" :*/
fileprivate let engineByMessage:String = "network us disappear headgetIn"
fileprivate let constArrowResult:String = "dAppshared require phone search boot"
fileprivate let const_statusName:String = "level"

/*: "getSystemUUID" :*/
fileprivate let configCreateLabFormat:String = "getSsuper decide create large document"
fileprivate let engineStigmatiseID:String = "mUUIDcancel domain base activity"

/*: "getCountryCode" :*/
fileprivate let noti_tagKitError:String = "getCounvisible off end"
fileprivate let app_decideDomainMessage:String = "kind nativetryCode"

/*: "inAppRating" :*/
fileprivate let controllerMainToken:String = "inAppRmap style break"

/*: "apPay" :*/
fileprivate let netReceiveSourceFormat:String = "apPayhand scheme no"

/*: "subscribe" :*/
fileprivate let show_backAfterName:[Character] = ["s","u","b","s","c","r","i"]
fileprivate let show_levelFormat:[Character] = ["b","e"]

/*: "openSystemBrowser" :*/
fileprivate let data_screenStatus:String = "openShide foundation boot"
fileprivate let cacheTingDecisionState:String = "sub with formBro"

/*: "closeWebview" :*/
fileprivate let networkCutOutWithURL:[Character] = ["c","l","o","s","e","W","e","b","v"]
fileprivate let userSharedName:[Character] = ["i","e","w"]

/*: "openNewWebview" :*/
fileprivate let loggerAtMessage:String = "odisplayn"
fileprivate let bridgeZoneSecret:String = "rating"
fileprivate let loggerSystemToolName:String = "bvieresponse"

/*: "reloadWebview" :*/
fileprivate let modelWaitMode:String = "rgoloa"
fileprivate let helperMessageTitle:[Character] = ["v","i","e","w"]

/*: "typeName" :*/
fileprivate let enginePostToken:[UInt8] = [0x51,0x5c,0x55,0x40,0x6b,0x44,0x48,0x40]

private func categoryM(image num: UInt8) -> UInt8 {
    return num ^ 37
}

/*: "deviceID" :*/
fileprivate let showDeletePendingID:[UInt8] = [0x86,0x87,0x94,0x8b,0x81,0x87,0xab,0xa6]

private func previousCenter(service num: UInt8) -> UInt8 {
    return num ^ 226
}

/*: "fireBaseID" :*/
fileprivate let noti_streetSmartDate:[UInt8] = [0x63,0x66,0x6f,0x62,0x3f,0x5e,0x70,0x62,0x46,0x41]

fileprivate func getWeaving(item num: UInt8) -> UInt8 {
    let value = Int(num) + 3
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "areaISO" :*/
fileprivate let noti_activeTitle:[UInt8] = [0x6c,0x7f,0x68,0x6c,0x44,0x5e,0x42]

private func flexiblePresentation(product num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "isProxy" :*/
fileprivate let modelSoundState:[UInt8] = [0xa0,0xba,0x99,0xbb,0xa6,0xb1,0xb0]

/*: "langCode" :*/
fileprivate let kMakeVersion:[UInt8] = [0x89,0x84,0x8b,0x82,0xa6,0x8a,0x81,0x80]

private func titleBe(activity num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "timeZone" :*/
fileprivate let managerProposalMode:[UInt8] = [0x6,0x1b,0x1f,0x17,0x28,0x1d,0x1c,0x17]

private func againOkBehavior(image num: UInt8) -> UInt8 {
    return num ^ 114
}

/*: "installedApps" :*/
fileprivate let showAppearCount:[UInt8] = [0x2e,0x29,0x34,0x33,0x26,0x2b,0x2b,0x22,0x23,0x6,0x37,0x37,0x34]

private func automobileFactory(clear num: UInt8) -> UInt8 {
    return num ^ 71
}

/*: "systemUUID" :*/
fileprivate let cacheDelayName:[UInt8] = [0x70,0x7a,0x70,0x77,0x66,0x6e,0x56,0x56,0x4a,0x47]

private func errorList(mirror num: UInt8) -> UInt8 {
    return num ^ 3
}

/*: "countryCode" :*/
fileprivate let loggerSecondFlag:[UInt8] = [0x1c,0x10,0xa,0x11,0xb,0xd,0x6,0x3c,0x10,0x1b,0x1a]

/*: "status" :*/
fileprivate let formatterGrantProtectionValue:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x73]

/*: "isAuth" :*/
fileprivate let formatterRequestProductPath:[UInt8] = [0xbd,0xa7,0x95,0xa1,0xa0,0xbc]

private func schemeFifty(event num: UInt8) -> UInt8 {
    return num ^ 212
}

/*: "isFirst" :*/
fileprivate let managerNetworkName:[UInt8] = [0x9a,0xa4,0x77,0x9a,0xa3,0xa4,0xa5]

fileprivate func carrierExecute(always num: UInt8) -> UInt8 {
    let value = Int(num) - 49
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: __LocalPush" :*/
fileprivate let k_sinceCount:String = "__Lockey window mic evaluate reduce"
fileprivate let show_tooStr:String = "SH"

/*: "identifier" :*/
fileprivate let engineLevelTagRefreshCount:[UInt8] = [0xee,0xe3,0xe2,0xe9,0xf3,0xee,0xe1,0xee,0xe2,0xf5]

/*: "HTTPProxy" :*/
fileprivate let engineSpaceFlag:String = "HTTPProxyoff take"

/*: "HTTPSProxy" :*/
fileprivate let engineProcessDate:[Character] = ["H","T","T","P","S","P","r","o","x","y"]

/*: "SOCKSProxy" :*/
fileprivate let sessionCornerFormat:String = "so"
fileprivate let userMagnitudePath:[Character] = ["C","K","S","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let notiEngineTapDate:String = "language"
fileprivate let managerRootMessage:String = "never true else close contact_SCOP"

/*: "tap" :*/
fileprivate let app_loadTime:String = "tacontext"

/*: "tun" :*/
fileprivate let serviceControlValue:String = "ben"

/*: "ipsec" :*/
fileprivate let appWhiteFormat:String = "icontentec"

/*: "ppp" :*/
fileprivate let mainTaprootOriginalTitle:[Character] = ["p","p","p"]

/*: "Retry After or Go to " :*/
fileprivate let data_actionDismissPath:[Character] = ["R","e","t","r","y"," ","A","f","t"]
fileprivate let main_deviceSkiMessage:[Character] = ["e","r"," ","o","r"," ","G","o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let managerExcessivelyStatus:String = "Feedbackcenter stop instance index"

/*: " to contact us" :*/
fileprivate let notiRelationString:String = " to ski dismiss take"
fileprivate let networkSinceString:[Character] = ["c","o","n","t","a","c","t"," ","u","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

/// H5事件
//: private let getDeviceID     = "getDeviceID"        // 获取设备号
private let kPauseFormat = (String(app_backError.prefix(5)) + "viceID") // 获取设备号
//: private let getFirebaseID   = "getFirebaseID"      // 获取FireBaseToken
private let userNumberCount = (String(modelFirstData)) // 获取FireBaseToken
//: private let getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
private let constInputKey = (String(servicePriceResult)) // 获取 SIM/运营商 地区ISO
//: private let getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
private let cacheMacDate = (String(modelComponentPartCount.prefix(4)) + "roxyS" + parserFoundationMessage.replacingOccurrences(of: "evaluate", with: "s")) // 获取是否使用了代理
//: private let getMicStatus    = "getMicStatus"       // 获取麦克风权限
private let factoryTrustSupportMessage = (String(sessionPreviousError.suffix(7)) + String(sessionOkName)) // 获取麦克风权限
//: private let getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
private let configDoingceName = (String(serviceReduceStr) + configOriginSourceTitle.lowercased() + String(dataAppearStatus.prefix(7))) // 获取相册权限
//: private let getCameraStatus = "getCameraStatus"    // 获取相机权限
private let kFreshValue = (String(constSoundSecretName.suffix(5)) + "meraSt" + String(dataTapKeyFormat)) // 获取相机权限
//: private let reportAdjust    = "reportAdjust"       // 上报Adjust
private let mainRequestURL = (const_programState.replacingOccurrences(of: "system", with: "e") + "rtAdjust") // 上报Adjust
//: private let requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
private let notiDeviceState = (serviceBounceString.replacingOccurrences(of: "execute", with: "ue") + "tLoc" + String(appCornerList)) // APP在后台收到音视频通话推送
//: private let getLangCode      = "getLangCode"        // 获取系统语言
private let main_skiBootCount = (String(kMasterKeyTime) + String(kWaitMessage)) // 获取系统语言
//: private let getTimeZone      = "getTimeZone"        // 获取当前系统时区
private let routerBackgroundMessage = (String(userSubData) + appStopData.lowercased()) // 获取当前系统时区
//: private let getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
private let notiRequirePath = (String(engineByMessage.suffix(5)) + "stalle" + String(constArrowResult.prefix(4)) + const_statusName.replacingOccurrences(of: "level", with: "s")) // 获取已安装应用列表
//: private let getSystemUUID    = "getSystemUUID"      // 获取系统UUID
private let loggerOpenValueResult = (String(configCreateLabFormat.prefix(4)) + "yste" + String(engineStigmatiseID.prefix(5))) // 获取系统UUID
//: private let getCountryCode   = "getCountryCode"     // 获取当前系统地区
private let viewPromptCount = (String(noti_tagKitError.prefix(7)) + String(app_decideDomainMessage.suffix(7))) // 获取当前系统地区
//: private let inAppRating      = "inAppRating"        // 应用内评分
private let viewLadenPath = (String(controllerMainToken.prefix(6)) + "ating") // 应用内评分
//: private let apPay            = "apPay"              // 苹果支付
private let notiTeaserStatus = (String(netReceiveSourceFormat.prefix(5))) // 苹果支付
//: private let subscribe        = "subscribe"          // 苹果支付-订阅
private let showRogueStateState = (String(show_backAfterName) + String(show_levelFormat)) // 苹果支付-订阅
//: private let openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
private let cacheSumervalMessage = (String(data_screenStatus.prefix(5)) + "yste" + String(cacheTingDecisionState.suffix(4)) + "wser") // 调用系统浏览器打开url
//: private let closeWebview     = "closeWebview"       // 关闭当前webview
private let serviceBackgroundSinceData = (String(networkCutOutWithURL) + String(userSharedName)) // 关闭当前webview
//: private let openNewWebview   = "openNewWebview"     // 使用新webview打开url
private let engineNumberPriceURL = (loggerAtMessage.replacingOccurrences(of: "display", with: "pe") + "NewW" + bridgeZoneSecret.replacingOccurrences(of: "rating", with: "e") + loggerSystemToolName.replacingOccurrences(of: "response", with: "w")) // 使用新webview打开url
//: private let reloadWebview    = "reloadWebview"      // 重载webView
private let configPresentTime = (modelWaitMode.replacingOccurrences(of: "go", with: "e") + "dWeb" + String(helperMessageTitle)) // 重载webView

//: struct JSMessageModel: HandyJSON {
struct SimulationModel: HandyJSON {
    //: var typeName = ""
    var typeName = ""
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: PlacementDelay?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct PlacementDelay: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension FirstThread {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func beyondIncentiveProgramConfirm(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = SimulationModel.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case getDeviceID:
            case kPauseFormat:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = LiveWarning.readjustShow()
                //: callBack(["typeName": model.typeName, "deviceID": adidStr])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: showDeletePendingID.map{previousCenter(service: $0)}, encoding: .utf8)!: adidStr])

            //: case getFirebaseID:
            case userNumberCount:
                //: AppWebViewController.getFireBaseToken { str in
                FirstThread.name { str in
                    //: callBack(["typeName": model.typeName, "fireBaseID": str])
                    callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: noti_streetSmartDate.map{getWeaving(item: $0)}, encoding: .utf8)!: str])
                }

            //: case getAreaISO:
            case constInputKey:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = FirstThread.revenue()
                //: callBack(["typeName": model.typeName, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: noti_activeTitle.map{flexiblePresentation(product: $0)}, encoding: .utf8)!: arr.joined(separator: ",")])

            //: case getProxyStatus:
            case cacheMacDate:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = FirstThread.adjust()
                //: callBack(["typeName": model.typeName, "isProxy": status])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: modelSoundState.map{$0^201}, encoding: .utf8)!: status])

            //: case getLangCode:
            case main_skiBootCount:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName, "langCode": langCode])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: kMakeVersion.map{titleBe(activity: $0)}, encoding: .utf8)!: langCode])

            //: case getTimeZone:
            case routerBackgroundMessage:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName, "timeZone": timeZone])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: managerProposalMode.map{againOkBehavior(image: $0)}, encoding: .utf8)!: timeZone])

            //: case getInstalledApps:
            case notiRequirePath:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName, "installedApps": apps])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: showAppearCount.map{automobileFactory(clear: $0)}, encoding: .utf8)!: apps])

            //: case getSystemUUID:
            case loggerOpenValueResult:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName, "systemUUID": uuid])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: cacheDelayName.map{errorList(mirror: $0)}, encoding: .utf8)!: uuid])

            //: case getCountryCode:
            case viewPromptCount:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName, "countryCode": countryCode])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: loggerSecondFlag.map{$0^127}, encoding: .utf8)!: countryCode])

            //: case inAppRating:
            case viewLadenPath:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.requestInAppRating()
                FirstThread.add()

            //: case apPay:
            case notiTeaserStatus:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.localOff(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: formatterGrantProtectionValue.reversed(), encoding: .utf8)!: success])
                    }
                }

            //: case subscribe:
            case showRogueStateState:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.localOff(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName, "status": success])
                        callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: formatterGrantProtectionValue.reversed(), encoding: .utf8)!: success])
                    }
                }

            //: case openSystemBrowser:
            case cacheSumervalMessage:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case closeWebview:
            case serviceBackgroundSinceData:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName])
                //: self.closeWeb()
                self.offSetCapture()

            //: case openNewWebview:
            case engineNumberPriceURL:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    FirstThread.hand(urlStr, transparency, fullscreen)
                }

            //: case reloadWebview:
            case configPresentTime:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName])
                //: self.reloadWebView()
                self.component()

            //: case getMicStatus:
            case factoryTrustSupportMessage:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                MightHaveBeenThird.shared.global { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: formatterRequestProductPath.map{schemeFifty(event: $0)}, encoding: .utf8)!: auth, String(bytes: managerNetworkName.map{carrierExecute(always: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getPhotoStatus:
            case configDoingceName:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                MightHaveBeenThird.shared.chase { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: formatterRequestProductPath.map{schemeFifty(event: $0)}, encoding: .utf8)!: auth, String(bytes: managerNetworkName.map{carrierExecute(always: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case getCameraStatus:
            case kFreshValue:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                MightHaveBeenThird.shared.push { auth, isFirst in
                    //: callBack(["typeName": model.typeName, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName, String(bytes: formatterRequestProductPath.map{schemeFifty(event: $0)}, encoding: .utf8)!: auth, String(bytes: managerNetworkName.map{carrierExecute(always: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case reportAdjust:
            case mainRequestURL:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        LiveWarning.collection(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        LiveWarning.occurrence(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName])

            //: case requestLocalPush:
            case notiDeviceState:
                //: callBack(["typeName": model.typeName])
                callBack([String(bytes: enginePostToken.map{categoryM(image: $0)}, encoding: .utf8)!: model.typeName])
                //: AppWebViewController.pushLocalNotification(model)
                FirstThread.continuity(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension FirstThread {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func hand(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = FirstThread()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        PartDelayImmediate.message()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func continuity(_ model: SimulationModel) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(show_tooVersion)" + (String(k_sinceCount.prefix(5)) + "alPu" + show_tooStr.lowercased())
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: engineLevelTagRefreshCount.map{$0^135}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func name(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func revenue() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func adjust() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if FirstThread.openceZonePrompt() || FirstThread.drop() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func openceZonePrompt() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(String(engineSpaceFlag.prefix(9)))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(String(engineProcessDate))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(sessionCornerFormat.uppercased() + String(userMagnitudePath))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func drop() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(notiEngineTapDate.replacingOccurrences(of: "language", with: "_") + String(managerRootMessage.suffix(5)) + "ED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((app_loadTime.replacingOccurrences(of: "context", with: "p"))) || keyStr.contains((serviceControlValue.replacingOccurrences(of: "be", with: "tu"))) || keyStr.contains((appWhiteFormat.replacingOccurrences(of: "content", with: "ps"))) || keyStr.contains((String(mainTaprootOriginalTitle))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func add() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    // MARK: - Event

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func localOff(productId: String, source: Int = -1, payType: PriorityCamera, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        OfExample.product()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        AdornPresentation.shared.decision(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            OfExample.by()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us")
                    OfExample.usedProgress((String(data_actionDismissPath) + String(main_deviceSkiMessage)) + "\"" + (String(managerExcessivelyStatus.prefix(8))) + "\"" + (String(notiRelationString.prefix(4)) + String(networkSinceString)))

                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.sharedProgress()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
